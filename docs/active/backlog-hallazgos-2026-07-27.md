# Backlog de hallazgos — 2026-07-27

Hallazgos detectados durante el trabajo de moneda primaria y rigor de cálculo
(rama `fix/moneda-primaria-y-rigor-de-calculo`). **Ninguno fue arreglado**: la regla de
alcance congelado obligaba a anotarlos en vez de tocarlos, que es lo que permitió que
ese trabajo convergiera.

Estado: **sin verificar**. Vienen del agente que los encontró mientras arreglaba otra
cosa; a diferencia de los 37 que sí pasaron por triage adversarial, éstos no tienen
repro confirmado. Antes de arreglar cualquiera, verificarlo (ver
[continuación](continuacion-moneda-y-calculo.md), paso 3).

Formato: `- [paquete] título — archivo:línea — por qué importa`

---

- [P1-frontend] `allowLowMargin` no tiene ningún setter en la UI — frontend/src/context/QuoteBuilderContext.tsx:34 — el error 'CRITICAL: Price below Cost' apaga isValid y bloquea el guardado sin vía de escape (misma clase de defecto que H13, que sí estaba en alcance); el único modo de encenderlo es updateProjectInfo({allowLowMargin:true}) y nadie lo llama.
- [P1-frontend] addExpense/updateExpense calculan clientPrice en el front con aritmética float — frontend/src/context/QuoteBuilderContext.tsx:311-329 — el propio tipo QuoteExpense dice "computed by backend" (types/quote-builder.ts:101); el valor local puede diferir del que devuelve POST /quotes/calculate y es el que se muestra en la fila del gasto.
- [P1-frontend] El panel de errores rotula todo como campos faltantes — frontend/src/components/quotes/builder/QuoteBuilderForm.tsx:216 — dice "Faltan N campos antes de crear la propuesta" aunque los errores incluyen cosas que no son campos (p. ej. 'CRITICAL: Price below Cost'), lo que confunde sobre qué hay que corregir.
- [P1-frontend] `apiRequest` hace `fetch` sin AbortSignal ni timeout — frontend/src/lib/api-client.ts:77 — es la causa raíz de H18: con el backend colgado a nivel TCP ningún request settlea nunca, así que `loading` (useAuth.ts:117) y `state.isHydrated` (NougramCoreContext.tsx:149) quedan trabados y toda pantalla que espere una respuesta gira para siempre. La ruta '/' ya tiene techo propio, pero el resto de la app no. Fix real: `AbortSignal.timeout(N)` en api-client.ts (archivo fuera de archivos_propios de este paquete).
- [P2-moneda] `calculate_sales_projection` sigue defaulteando a `currency="USD"` en su firma — backend/app/services/sales_projection_service.py:28 — H31 se cerró en el endpoint (única ruta HTTP), pero el servicio sigue aceptando cualquier moneda y usándola como target de normalización del BCR; cualquier caller nuevo repite el bug. El archivo no es de este paquete.
- [P2-moneda] `EXCHANGE_RATES_TO_USD` (dict de float) sigue vivo y se usa en caminos monetarios — backend/app/api/v1/endpoints/costs.py:594-601 y backend/app/services/exchange_rate_service.py:62/80/89/110 — hacen `Decimal(str(float_rate))`, o sea round-trip por float de una tasa que ya existe en Decimal (`EXCHANGE_RATES_TO_USD_DECIMAL`). Viola el estándar de dinero; los dos archivos son de otros paquetes.
- [P2-moneda] La rama `org.country` de `resolve_primary_currency` es código muerto — backend/app/core/currency.py:248 vs backend/app/models/organization.py:35-52 — el modelo Organization no declara columna `country`, así que `getattr(org,'country',None)` siempre da None para objetos ORM. O se agrega la columna o se borra la rama; ambas cosas requieren tocar el modelo (fuera de archivos_propios).
- [P2-moneda] `SettingsService._convert_decimal` levanta HTTP 400 donde el core convierte en silencio — backend/app/services/settings_service.py:49-55 — el core (currency.py) y el servicio que lo envuelve tienen políticas distintas para la misma moneda no soportada; conviene unificar en una sola política ahora que el core al menos loguea. settings_service.py es de P3.
- [P2-moneda] `annual_sales_projection_service` hardcodea "USD" como moneda de fallback de filas — backend/app/services/annual_sales_projection_service.py:121 y :141 — `cost.currency or "USD"` / `member.currency or "USD"` en vez de `DEFAULT_CURRENCY` o de la moneda primaria de la org; con una fila sin moneda en una org COP, el importe se divide por 4000. Es preexistente y no estaba en ningún hallazgo del paquete.
- [P2-moneda] La derivación de moneda por país en el onboarding es código inalcanzable — backend/app/services/onboarding_service.py:700-707 vs backend/app/schemas/onboarding.py:99 — `effective_currency = request_currency or resolve_primary_currency({...,'country':...})`, pero `currency: Currency = Field(...)` es OBLIGATORIA en CompleteOnboardingRequest y :655 hace `getattr(request.currency,'value',...).upper()`, siempre truthy. La rama `or` nunca corre: por eso H15 detectó que ningún test la ejercita — no es falta de test, es código muerto. O se hace `currency` opcional o se borra la rama. Ninguno de los dos archivos es de este paquete.
- [P3-totales-cotizacion] Los expenses del builder se pierden al CREAR el proyecto — frontend/src/services/quoteService.ts:534-558 y backend/app/schemas/project.py:289-307 — `create()` no manda `expenses` en el POST /projects/ y `ProjectCreateWithQuote` no tiene el campo, así que los gastos de terceros cargados en el builder no se persisten en el primer guardado (recién aparecen si el usuario vuelve a guardar, que ya usa PUT). Por eso project_service.py:343 puede seguir pasando `expenses=None`: no hay nada que sumar. Arreglarlo pide tocar el schema y el front, ninguno de este paquete.
- [P3-totales-cotizacion] El PDF, el DOCX y el análisis de rentabilidad siguen gravando la contingencia — backend/app/core/pdf_generator.py:49-68, backend/app/core/docx_generator.py:51-65 y backend/app/core/calculations.py:773-793 — calculan `tax_amount = quote.total_client_price * pct/100` sobre un precio que ya incluye la contingencia. El resto de los caminos de lectura ya usa `compute_quote_tax_totals()` (app/services/project_service.py); estos tres necesitan la misma llamada de una línea, pero los tres archivos son de otros paquetes (resto de H43).
- [P3-totales-cotizacion] `QuoteExpense` no guarda la moneda de la fila — backend/app/models/project.py (tabla quote_expenses) — al convertir un proyecto de moneda se asume que el expense está en `project.currency`; si alguna vez se permite cargar un gasto en otra moneda, la conversión lo multiplica mal y no hay forma de detectarlo. Requiere migración + modelo, fuera de este paquete.
- [P3-totales-cotizacion] PDF y DOCX gravan la contingencia con el impuesto — app/core/pdf_generator.py:56 y app/core/docx_generator.py:53 — `tax_amount = total_client_price * tax.percentage / 100` sobre quote.total_client_price, que ya viene contingenciado. El camino de lectura (GET /projects/{p}/quotes/{q}) y el preview usan compute_quote_tax_totals (base sin contingencia), así que los documentos que ve el cliente imprimen un Total mayor que el que la app muestra en pantalla (delta = tasa × contingencia; con 1.000.000 + 10% + IVA 19% son 1.309.000 vs 1.290.000). Fix: usar app.services.project_service.compute_quote_tax_totals(quote.total_client_price, quote.contingency_type, quote.contingency_value, [...]) en ambos generadores. NO se tocó porque esos dos archivos no son del paquete; para no dejar un mail cuyo cuerpo y cuyo adjunto dicen plata distinta, project_service.send_quote_email volvió a replicar la regla vieja de los generadores. Cuando se arreglen los generadores hay que cambiar send_quote_email a compute_quote_tax_totals en la misma pasada (el comentario en project_service.py lo dice) y ajustar tests/integration/test_p3_rescue_regressions.py::TestQuoteEmailBodyMatchesItsAttachment, que sólo exige que los dos números coincidan.
- [P4-bcr-cargas] `activeQuotesCount` del dashboard cuenta una fila por VERSIÓN de presupuesto — backend/app/api/v1/endpoints/dashboard.py:143-162 — `select(func.count(Quote.id)).join(Project)` sobre proyectos en estado 'Sent' sin filtrar `Quote.is_active` ni quedarse con la última versión: un proyecto enviado con 3 versiones cuenta como 3 cotizaciones activas (y activeQuotesChange compara dos períodos igualmente inflados). Es la misma raíz que H30 pero en otro KPI; H30 sólo cubría totalRevenue/averageTicket, que ya quedaron con una cotización por proyecto (`_won_projects_revenue`). Fix: reusar la misma selección por proyecto.
- [P4-bcr-cargas] Precedencia invertida entre las dos resoluciones de cargas sociales — backend/app/core/business_health.py:27-53 vs backend/app/core/calculations.py:95-98 — `_social_charges_multiplier` toma primero la config de la DB y sólo usa el parámetro si la DB no tiene nada, mientras `calculate_blended_cost_rate` ahora hace lo contrario (el parámetro es override de simulación y la DB es el fallback). Con la misma llamada las dos funciones pueden aplicar recargos distintos si alguna vez se pasa una config simulada. Hoy no muerde porque todos los call sites pasan exactamente la config de la DB. Unificar en un solo helper, pero eso toca call sites de otros paquetes.
- [P5-ia] /ai/suggest-config toma `currency` del body sin validar ni contrastar con la org — backend/app/api/v1/endpoints/ai.py:245 (schema: app/schemas/ai.py:23, `str = Field(default="USD")`) — mismo patrón que H31: la IA sugiere sueldos "de la región" en una moneda que el cliente elige libremente y que puede no ser la primaria del tenant; si el usuario acepta las sugerencias, se persisten con esa moneda.
- [P5-ia] La sync de Google Sheets castea importes monetarios a float — backend/app/core/sheets.py:143,152,186,201 (`float(row.get("amount_monthly"/"salary_monthly_brute"))` sobre columnas Numeric(19,4)) — viola el estándar Decimal del proyecto e introduce error de redondeo binario en sueldos y costos fijos importados.
- [P5-ia] El prompt de IA suma `total_price` de los proyectos sin moneda ni normalización — backend/app/services/ai_service.py:152 ("Ingresos totales") sobre context["projects"] armado en ai.py:945-960 desde Quote.total_client_price — la tabla `quotes` no tiene columna currency y project.currency puede diferir de la primaria (ver H45), así que el ingreso total del prompt mezcla unidades y sale sin rótulo de moneda.
