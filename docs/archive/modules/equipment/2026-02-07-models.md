# Equipment Amortization - Modelo

**Archivo:** `backend/app/models/equipment.py`

## Modelo EquipmentAmortization

```python
from sqlalchemy import Column, Integer, String, Numeric, Date, Boolean, ForeignKey, DateTime
from sqlalchemy.orm import relationship
from app.models.base import BaseModel

class EquipmentAmortization(BaseModel):
    """
    Equipment Amortization Model
    
    NO debe contener queries SQL ni lógica de negocio.
    Solo definición de estructura ORM.
    """
    __tablename__ = "equipment_amortization"
    
    # Campos básicos
    name = Column(String, nullable=False)
    description = Column(String)
    category = Column(String, nullable=False)  # Hardware, Software, Vehicles, Office Equipment
    
    # Información financiera
    purchase_price = Column(Numeric(15, 2), nullable=False)
    purchase_date = Column(Date, nullable=False)
    currency = Column(String(3), nullable=False)
    exchange_rate_at_purchase = Column(Numeric(10, 4))  # ⚠️ CRÍTICO: TRM histórica
    
    # Parámetros de depreciación
    useful_life_months = Column(Integer, nullable=False)
    salvage_value = Column(Numeric(15, 2), default=0)  # ⚠️ CRÍTICO: Valor de salvamento
    depreciation_method = Column(String, nullable=False)  # "straight_line" o "declining_balance"
    
    # Estado
    is_active = Column(Boolean, default=True)
    
    # Relaciones
    organization_id = Column(Integer, ForeignKey("organizations.id"), nullable=False)
    deleted_by_id = Column(Integer, ForeignKey("users.id"), nullable=True)
    
    # Soft delete
    deleted_at = Column(DateTime, nullable=True)
    
    # Relationships
    organization = relationship("Organization", back_populates="equipment")
    deleted_by = relationship("User", foreign_keys=[deleted_by_id])
```

## Campos Críticos

### exchange_rate_at_purchase
- **Tipo:** `Numeric(10, 4)`
- **Nullable:** Sí
- **Propósito:** Almacenar TRM histórica al momento de compra
- **Uso:** Conversión a moneda principal en BCR sin re-expresión mensual

### salvage_value
- **Tipo:** `Numeric(15, 2)`
- **Default:** 0
- **Propósito:** Valor residual del equipo al final de su vida útil
- **Impacto:** Reduce BCR en ~15% al considerar valor residual

## Índices Requeridos

```python
# En migración Alembic
Index('ix_equipment_amortization_id', EquipmentAmortization.id)
Index('ix_equipment_amortization_organization_id', EquipmentAmortization.organization_id)
Index('ix_equipment_amortization_category', EquipmentAmortization.category)
Index('ix_equipment_amortization_deleted_at', EquipmentAmortization.deleted_at)
Index('ix_equipment_amortization_is_active', EquipmentAmortization.is_active)
```

## Referencias

- **BaseModel**: [`../../../../backend/app/models/base.py`](../../../../backend/app/models/base.py)
- **Arquitectura Models**: [`../../architecture/mvc/models.md`](../../architecture/mvc/models.md)
- **Migración**: [migration.md](./migration.md)
